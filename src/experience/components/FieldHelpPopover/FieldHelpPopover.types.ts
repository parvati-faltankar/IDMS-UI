export type FieldHelpPopoverProps = {
  /** Short label for the field — shown as the popover heading. */
  title: string;
  /** Clear explanation of what the field means and when to use it. */
  description: string;
  /** Optional concrete example shown in a highlighted block. */
  example?: string;
};
